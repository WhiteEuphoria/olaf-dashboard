<?php

return [
    // Toggle demo/theme routes like /user, /enter, and static admin views.
    // Enabled by default so the custom theme is active out of the box.
    'theme_routes' => env('ENABLE_THEME_ROUTES', true),
];
